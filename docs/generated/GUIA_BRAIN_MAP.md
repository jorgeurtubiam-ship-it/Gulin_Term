# GUÍA BRAIN MAP - GuLiN

## 1. Registro de Nodos
`brain_register_node(id, label, type, ...)`
Tipos: `core`, `skill`, `memory`, `server`, `db`, `aws-ec2`.

## 2. Conexiones
`brain_connect_nodes(source, target)`
Conecta nodos para visualizar relaciones.

## 3. Infraestructura
Registrada dinámicamente según la exploración. Consultar `brain-map.json` para estado actual.
