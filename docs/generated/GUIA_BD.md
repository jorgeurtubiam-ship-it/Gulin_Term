# GUÍA BASES DE DATOS - GuLiN

## 1. Conexiones Registradas
- `gulin_db_pkg` (sqlite)
- `gulin_db_root` (sqlite)
- `gulin_prod` (sqlite)
- `carnes_pro` (mssql)

## 2. Operaciones
- `db_query(connection_name, sql)` para SQL.
- `db_query(connection_name, collection, filter)` para MongoDB.
